// DFS Traversal

// 1 Time
void dfs(vector<int>>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);

    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(auto x :graph(source)){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 2 Time
void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);
    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visietd[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 3 Time
void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);
    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 4 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);

    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 5 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);

    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 6 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);
    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 7 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);
    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 8 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);
    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 9 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);
    for(int i=0;i<n;i++){
        if(visited[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}

// 10 Time

void dfs(vector<int>& graph){
    int n=graph.size();
    vector<bool> visited(n,false);

    for(int i=0;i<n;i++){
        if(viisted[i]==false){
            dfsr(graph,i,visited);
        }
    }
}

void dfsr(vector<int>& graph,int source,vector<bool>& visited){
    visited[source]=true;
    cout<<source<<" ";
    for(int x:graph[source]){
        if(visited[x]==false){
            dfsr(graph,x,visited);
        }
    }
}